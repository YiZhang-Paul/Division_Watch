export class ValueChange {
    public change: number;
    public duration: number;

    constructor(change: number, duration: number) {
        this.change = change;
        this.duration = duration;
    }
}
